# API Services

> **This file is outdated.** The authoritative API reference is `services/api/CLAUDE.md`.
> Endpoint contracts, middleware stack, and response shapes are documented there.
> Cross-module mapping (which mobile files call which endpoints) is in `packages/docs/cross-reference.md`.

Do not update this file. Update `services/api/CLAUDE.md` directly when the API changes.
